-Compilar:
	make
-Limpar:
	make clean
-Executar cliente (executável no directório user):
	./user [-n TCSname] [-p TCSport]
-Executar servidor TCS (executável no directório TCS):
	./tcs [-p TCSport]
-Executar servidor TRS (correr executável no directório TRS):
	./tcs language [-p TRSport] [-n TCSname] [-e TCSport]
